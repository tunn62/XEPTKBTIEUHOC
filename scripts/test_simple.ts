import { ALL_CLASSES, TIME_SLOTS, GVCN_MAP } from '../src/data/initialData';
import { isDiem1 } from '../src/solver/cspSolver';

console.log('Total classes:', ALL_CLASSES.length);
console.log('Total slots:', TIME_SLOTS.length);
